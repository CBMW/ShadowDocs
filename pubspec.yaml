name: shadowdocs
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15
  share_plus: ^7.0.0
  provider: ^6.0.5
  uuid: ^3.0.6
  introduction_screen: ^3.1.3
  flutter_svg: ^2.0.7
  path_provider: ^2.0.11
  flutter_markdown: ^0.6.10
  flutter_webrtc: ^0.9.0
  pdf: ^3.9.0
  printing: ^5.9.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true